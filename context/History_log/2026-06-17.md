# 2026-06-17

## HH:MM — Plan A: Display-layer dedup tighten (loose-key v4)

### สิ่งที่ทำ
- ย้าย loose-key v4 จาก `scripts/find-duplicate-faculties.ts` เข้าเป็น shared helper ที่ `src/lib/normalize-faculty.ts` (`looseProgram`, `looseDetail`, `looseMajor`, `looseFacultyKey`)
- เปลี่ยน `deduplicateFacultyRows` ใน `src/server/queries.ts` ให้ใช้ `looseFacultyKey(row, uniName)` แทน strict key เดิม (name + normalizeProgram + normalizeMajor + normalizeDetail)
- เพิ่ม `university: { select: { name: true } }` ใน include ของ `getFacultiesByUniSlug` และ `getFacultiesByUniversityId` เพราะ loose-key ต้องใช้ uniName สำหรับ strip embedded uni suffix
- Verify ผ่าน HTTP (PowerShell):
  - COTMES: 99 faculties (ลดจาก 125 → save 26)
  - DPU: 33 (save 6)
  - Mahanakorn: 13 (save 2)
  - ตรงกับ detection report ทุก uni
- DB ไม่ถูกแตะต้อง — เป็น display-only dedup

### Files changed
- `src/lib/normalize-faculty.ts` — export `looseProgram`, `looseDetail`, `looseMajor`, `looseFacultyKey` + helper `stripEmbeddedUniSuffix`
- `src/server/queries.ts` — `deduplicateFacultyRows(rows, uniName)`; ทั้ง 2 callsites ส่ง uniName มาให้ + เพิ่ม `university.name` ใน include

### หมายเหตุ
- เป็น Plan A ตามการตัดสินใจ — fix UI-visible ทันทีโดยไม่แตะ DB
- /analyze/major (getMajorComparison) มี dedup-by-university ของตัวเองอยู่แล้ว ไม่ได้ใช้ deduplicateFacultyRows จึงไม่ได้รับผลกระทบ
- Plan B (full DB merge) ยัง pending — ดูรายการขั้นตอนที่ระบุไว้ใน session

---

## HH:MM — B.4: Importer loose-key fallback (prevent regression)

### สิ่งที่ทำ
- เพิ่ม **Path A.5 loose-key lookup** ใน `upsertFaculty` — รันก่อน Path B (slug create) เพื่อจับ wording drift ที่ Path A พลาด
- ขยาย `preloadFacultyIndex` ให้ทำ pass 2 อ่าน Faculty ทุก row (ไม่ใช่เฉพาะที่มี TcasScore + programCode) แล้ว index ด้วย `looseFacultyKey` — first-write wins กรณีมี legacy dup
- Path A.5 hit → `prisma.faculty.update` ปรับ canonical data + programCode ของปีล่าสุด แล้ว seed Path A cache เพื่อให้ row ถัดไปใน batch เดียวกัน hit Path A เลย
- Path B insert ใหม่ → seed loose cache key ด้วย เพื่อกัน dup ในไฟล์ CSV เดียวกัน
- Type-check ผ่าน (`tsc --noEmit`)

### Files changed
- `scripts/import-tcas.ts` — import `looseFacultyKey`; `preloadFacultyIndex` pass 2; `upsertFaculty` Path A.5

### หมายเหตุ
- Root cause ของ COTMES dup เดิม: Path A COTMES fallback (line 390-393) trigger เฉพาะตอน normMajor ไม่ว่าง → TCAS64 (majorName="") import หลัง TCAS69 ("ภาษาไทย") ไม่ match → fall through ไป Path B สร้างใหม่
- Loose-key index ใช้ key เดียวกับ display dedup → import ไม่สามารถสร้าง row ที่ display layer จะ merge อีก ("self-consistent")
- ยังไม่รันจริง — รอ TCAS70 หรือ re-import ครั้งถัดไป

---

## 11:16

### Files changed
```
context/History_log/2026-06-12.md
src/app/analyze/compare/major/[slug]/loading.tsx
src/app/analyze/compare/major/[slug]/page.tsx
src/app/analyze/compare/page.tsx
src/app/analyze/page.tsx
src/app/sitemap.ts
src/components/layout/header.tsx
src/components/layout/mobile-menu.tsx
src/components/ui/combobox.tsx
src/features/analyze/components/analyze-form.tsx
src/features/analyze/components/result-card.tsx
src/features/analyze/components/weighted-inputs.tsx
src/features/compare/components/compare-score-input.tsx
src/features/compare/components/compare-slot-picker.tsx
src/features/compare/components/major-comparison-table.tsx
src/lib/major-canonical.ts
src/lib/normalize-faculty.ts
src/lib/subjects.ts
src/server/queries.ts
```


---

## 11:40

### Files changed
```
context/History_log/2026-06-12.md
scripts/import-tcas.ts
src/app/analyze/compare/major/[slug]/loading.tsx
src/app/analyze/compare/major/[slug]/page.tsx
src/app/analyze/compare/page.tsx
src/app/analyze/page.tsx
src/app/sitemap.ts
src/components/layout/header.tsx
src/components/layout/mobile-menu.tsx
src/components/ui/combobox.tsx
src/features/analyze/components/analyze-form.tsx
src/features/analyze/components/result-card.tsx
src/features/analyze/components/weighted-inputs.tsx
src/features/compare/components/compare-score-input.tsx
src/features/compare/components/compare-slot-picker.tsx
src/features/compare/components/major-comparison-table.tsx
src/lib/major-canonical.ts
src/lib/normalize-faculty.ts
src/lib/subjects.ts
src/server/queries.ts
```


---

## 15:26

### Files changed
```
src/app/scores/[universitySlug]/[facultySlug]/page.tsx
src/server/queries.ts
src/types/tcas.ts
```


---

## 15:26

### Files changed
```
context/History_log/2026-06-17.md
src/app/scores/[universitySlug]/[facultySlug]/page.tsx
src/server/queries.ts
src/types/tcas.ts
```


---

## 17:22

### Files changed
```
context/History_log/2026-06-17.md
src/app/scores/[universitySlug]/[facultySlug]/page.tsx
src/app/scores/[universitySlug]/page.tsx
src/features/scores/components/faculty-list.tsx
src/server/queries.ts
src/types/tcas.ts
```

