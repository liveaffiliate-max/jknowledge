# History Log — 2026-06-12


---

## 10:12

### Files changed
```
src/app/page.tsx
src/features/tcas-folio/components/pdf-preview-card.tsx
```


---

## 10:50

### Files changed
```
src/app/page.tsx
src/app/sign-in/[[...sign-in]]/page.tsx
src/features/auth/lib/sso-finalize.ts
src/features/auth/lib/validation.ts
src/features/tcas-folio/components/pdf-preview-card.tsx
```


---

## 10:54

### Files changed
```
src/app/page.tsx
src/app/sign-in/[[...sign-in]]/page.tsx
src/app/sign-in/sso-callback/page.tsx
src/app/sign-up/[[...sign-up]]/page.tsx
src/app/sign-up/sso-callback/page.tsx
src/components/layout/header.tsx
src/components/layout/mobile-menu.tsx
src/features/auth/lib/sso-finalize.ts
src/features/auth/lib/validation.ts
src/features/tcas-folio/components/pdf-preview-card.tsx
```


---

## — Phase 0 + 1.1: Compare feature foundation

### สิ่งที่ทำ
- เพิ่ม type `CompareEntry` + ค่าคงที่ `COMPARE_MIN_SLOTS` / `COMPARE_MAX_SLOTS`
- เพิ่ม `latestMaxScore` / `latestSeats` ใน `AdmissionResult` เพื่อให้ compare table ใช้ได้ตรง ๆ ไม่ต้องดิ่งเข้า scores[]
- เพิ่ม server action `getMultipleAnalysisAction(entries[])` — batch analyze หลายคณะ (ไม่ save PredictionHistory เพราะ compare เป็น exploratory)
- สร้าง storage util `src/features/compare/lib/storage.ts` (`jknowledge:compare:v1`)
- Scaffold route `/analyze/compare` + `loading.tsx` พร้อม placeholder empty state (ยังไม่มี form จริง — รอ task 1.3+)

### Files changed
```
src/types/tcas.ts
src/server/actions.ts
src/features/compare/lib/storage.ts          (new)
src/app/analyze/compare/page.tsx             (new)
src/app/analyze/compare/loading.tsx          (new)
```


---

## 13:48

### Files changed
```
src/app/page.tsx
src/app/sign-in/[[...sign-in]]/page.tsx
src/app/sign-in/sso-callback/page.tsx
src/app/sign-up/[[...sign-up]]/page.tsx
src/app/sign-up/sso-callback/page.tsx
src/components/layout/header.tsx
src/components/layout/mobile-menu.tsx
src/features/auth/lib/sso-finalize.ts
src/features/auth/lib/validation.ts
src/features/tcas-folio/components/pdf-preview-card.tsx
src/server/actions.ts
src/types/tcas.ts
```


---

## — Phase 1 (1.2 → 1.10): Compare feature MVP

### สิ่งที่ทำ
- **Extract Combobox** จาก `analyze-form.tsx` มาเป็น shared `src/components/ui/combobox.tsx` ให้ compare reuse ได้ — analyze-form refactor ไม่กระทบ behavior
- **CompareSlotPicker** (1.3) — กล่องเลือก uni + คณะแบบ compact ใช้ Combobox ที่ extract มา รองรับ `initialUniversityId` ตอน hydrate
- **CompareScoreInput** (1.4) — กรอกคะแนนชุดเดียวกันใช้ทุก slot, render union ของ subject codes + fallback อันเดียวสำหรับคณะที่ไม่มี weights
- **CompareTable** (1.5/1.6/1.7) — grid 2-4 column desktop / stack mobile, highlight ช่องที่ gap สูงสุด (border-green + Trophy badge) / min ต่ำสุด / seats เยอะสุด, ไม่ใช้สีแดงตาม DESIGN.md, มี mini sparkline ของ minScore 6 ปี
- **CompareClient** orchestrator — รวม slot pickers + score input + submit + table + share, สนับสนุน URL `?ids=a,b,c` (Suspense-wrapped useSearchParams) และ sessionStorage persistence
- **`getFacultyMetaAction`** — server action ใหม่สำหรับ hydrate slot จาก facultyId เดี่ยวๆ โดยไม่ต้อง probe ทุก uni
- **ResultCard CTA** (1.2) — ปุ่ม "เปรียบเทียบกับคณะอื่น" link ไป `/analyze/compare?ids=<facultyId>`
- **CompareShareButton** (1.8) — copy URL หรือ navigator.share พร้อม analytics
- **Analytics events** (1.9) — `trackCompareAdd`, `trackCompareSubmit`, `trackCompareShare` ใส่ใน `src/lib/analytics.ts` พร้อม bucketing privacy

### Files changed
```
src/components/ui/combobox.tsx                                    (new)
src/features/analyze/components/analyze-form.tsx                  (Combobox extracted out)
src/features/analyze/components/result-card.tsx                   (compare CTA)
src/features/compare/components/compare-slot-picker.tsx           (new)
src/features/compare/components/compare-score-input.tsx           (new)
src/features/compare/components/compare-table.tsx                 (new)
src/features/compare/components/compare-share-button.tsx          (new)
src/features/compare/components/compare-client.tsx                (new)
src/app/analyze/compare/page.tsx                                  (wired CompareClient)
src/server/actions.ts                                             (getFacultyMetaAction)
src/lib/analytics.ts                                              (3 compare events)
```

### หมายเหตุ
- ตรวจด้วย `npx tsc --noEmit` — ผ่านหมด (เหลือ error เก่าของ `pdf-preview-card.tsx` Uint8Array<ArrayBufferLike> ที่ไม่เกี่ยว)
- ไม่สามารถ start preview server ได้เพราะ port 3000 ถูก dev server ของผู้ใช้ใช้อยู่ — Next.js App Router pick up ไฟล์ใหม่อัตโนมัติ ผู้ใช้ทดสอบที่ http://localhost:3000/analyze/compare ได้เลย


---

## 14:15

### Files changed
```
src/app/page.tsx
src/app/sign-in/[[...sign-in]]/page.tsx
src/app/sign-in/sso-callback/page.tsx
src/app/sign-up/[[...sign-up]]/page.tsx
src/app/sign-up/sso-callback/page.tsx
src/components/layout/header.tsx
src/components/layout/mobile-menu.tsx
src/features/analyze/components/analyze-form.tsx
src/features/analyze/components/result-card.tsx
src/features/auth/lib/sso-finalize.ts
src/features/auth/lib/validation.ts
src/features/tcas-folio/components/pdf-preview-card.tsx
src/lib/analytics.ts
src/server/actions.ts
src/types/tcas.ts
```


---

## — Compare: Multi-line cross-faculty trend chart

### สิ่งที่ทำ
- เพิ่ม `CompareTrendChart` — line chart รวมเทรนด์ minScore ย้อนหลังของทุก slot ในกราฟเดียว
  - 1 เส้น = 1 คณะ ใช้สีประจำมหาวิทยาลัย (fallback green/blue/purple/amber palette ถ้า uni ไม่มีสี)
  - Pivot ข้อมูลเป็น year-rows × slot-columns รองรับ slot ที่มีจำนวนปีไม่เท่ากัน
  - Y-axis scale ตาม union ของ min/max ± 3 padding
  - Custom tooltip เรียงคะแนนสูง→ต่ำ ให้รู้ว่าคณะไหนนำในปีนั้น
  - Legend แบบ chip คลิกเปิด/ปิดเส้น (isolate compare ได้)
- เพิ่ม `compare-trend-chart-lazy.tsx` — defer Recharts bundle จนกว่าจะมีผลลัพธ์
- เสียบใน CompareClient เป็น hero เหนือ CompareTable แสดงเมื่อมี ≥ 2 valid results

### Files changed
```
src/features/compare/components/compare-trend-chart.tsx        (new)
src/features/compare/components/compare-trend-chart-lazy.tsx   (new)
src/features/compare/components/compare-client.tsx
```

### หมายเหตุ
- ตรวจ `npx tsc --noEmit` ผ่าน (เหลือ error เดิมของ pdf-preview-card)
- ตัดสินใจไม่ใส่ user-score reference line ในกราฟนี้ เพราะแต่ละ slot คิด weighted score ด้วยน้ำหนักต่างกัน ค่า user score ในแต่ละ slot จึงไม่ใช่ค่าเดียวกัน — context ส่วนตัวยังคงอยู่ใน SlotCard ด้านล่าง


---

## — Compare chart: COTMES label fix + curated palette

### Problem
1. กสพท. (COTMES) ถูกเก็บใน DB เหมือนเป็น `University` ตัวหนึ่ง → ตอน compare เส้น 2 เส้น แสดง legend ว่า "กสพท. | COTMES" ซ้ำกัน ทำให้แยกไม่ออกว่าเป็นมหาลัยไหนจริง
2. ใช้ `university.color` เป็นสีเส้น → หลายมหาลัยมีสีคล้ายกัน (มก./มข./มจธ. แดงเหมือนกัน, KU/มข./ม.บูรพา เขียวคล้ายกัน) → เส้นซ้อนกันแยกไม่ออก

### สิ่งที่ทำ
- สร้าง `src/lib/faculty-label.ts`
  - `isCOTMES(uni)` — detect ด้วย `name.includes("กลุ่มสถาบัน")` หรือ `shortName.startsWith("กสพท")`
  - `getFacultyLabelParts(faculty)` — return `{ primaryShort, primaryName, channel? }`
    - กรณี COTMES: ดึงชื่อมหาลัยจริงจาก `faculty.name` ผ่าน regex map (22 มหาลัยสมาชิก กสพท) → `primaryShort = "จุฬาฯ"`, `primaryName = "คณะทันตแพทยศาสตร์"`, `channel = "กสพท"`
  - `COMPARE_PALETTE` — 4 สี categorical (green-600 / blue-600 / orange-600 / purple-600) แยกได้ชัด color-blind safe ใช้ทั้ง chart และ table
  - `paletteColor(idx)` — pick by slot index
- อัปเดต `compare-trend-chart.tsx`:
  - เปลี่ยน stroke color จาก brand → `paletteColor(idx)`
  - Legend chip + Tooltip ใช้ label parts ใหม่ พร้อมแสดง badge "กสพท" สีฟ้าอ่อนถ้าเป็นรอบ consortium
- อัปเดต `compare-table.tsx` SlotCard:
  - ใช้ label parts เดียวกัน — แสดง "จุฬาฯ" + "คณะทันตแพทยศาสตร์" แทน "กสพท. | COTMES · คณะ...จุฬาฯ"
  - เพิ่ม accent bar 4px ที่ขอบบน card สีตรงกับเส้นในกราฟ → จับคู่ภาพได้ทันที
  - ย้าย Trophy badge "เอื้อมถึง" เข้าใน header (จากเดิม -top-2 ที่ overlap กับ accent bar)

### Files changed
```
src/lib/faculty-label.ts                                       (new)
src/features/compare/components/compare-trend-chart.tsx
src/features/compare/components/compare-table.tsx
```

### หมายเหตุ
- ตรวจ `npx tsc --noEmit` ผ่านสะอาด (เหลือ error เดิมของ pdf-preview-card)
- ถ้าพบมหาลัยสมาชิก กสพท ใหม่ที่ยังไม่อยู่ใน `COTMES_MEMBER_MAP` → fallback แสดง shortName เดิม ("กสพท.") พร้อม badge — ไม่ crash


---

## — Score-aware everywhere: raw subject scores shared across /analyze*

### Problem
"คะแนน 65" ที่ user ได้ที่ /analyze ไม่สามารถใช้ข้ามไปวัดที่ /analyze/major หรือ /analyze/compare ได้ตรง ๆ เพราะแต่ละ faculty คิด weighted ด้วยน้ำหนักต่างกัน → ต้องเก็บ "คะแนนวิชาดิบ" แล้วคำนวณ weighted ใหม่ ต่อ faculty

### สิ่งที่ทำ
- **Shared storage** `src/lib/user-scores.ts`
  - `localStorage` key `jknowledge:user-scores:v1` — เก็บ `subjectScores: Record<code, string>` + `fallbackScore` + `updatedAt`
  - Legacy migration: ครั้งแรกที่อ่าน → ดึงจาก `jknowledge:analyze:v1` (sessionStorage) มาให้ผู้ใช้ที่เคยใช้แล้ว
  - Hook `useUserScores()` ใช้ `useSyncExternalStore` — reactive ระหว่าง component โดยไม่ต้อง React Context มี `setSubjectScore`, `setFallbackScore`, `clear`, `hasScores`
  - Same-tab notification ผ่าน custom event `jknowledge:user-scores` (storage event fire เฉพาะ cross-tab)
- **AnalyzeForm side-effect write** — เพิ่ม mirror write ไปยัง shared key ทุกครั้งที่ subjectScores/fallbackScore เปลี่ยน (merge กับของเดิม ไม่ overwrite วิชาที่ user กรอกจากหน้าอื่น)
- **getMajorComparison query** — รวม `FacultyRequirement` (include) เพิ่ม field `requirementWeights` ใน MajorComparisonEntry เพื่อให้ client คำนวณ weighted ของแต่ละ faculty ได้เอง
- **MyScoreCard component** — shared UI 2 mode:
  - **Summary mode** (no prop) — chips + edit/clear, used ที่ index `/analyze/major`
  - **Input mode** (`relevantSubjects` prop) — input grid + progress bar + ล้าง, used ที่ `/analyze/major/[slug]`
- **MajorComparisonTable** — เพิ่ม column "โอกาส" แทน "ตำแหน่ง" (ย้าย location ไป subtitle ของ uni)
  - `computeChance(entry, userScores, hasScores)` per row — 4 status: `no_user` / `no_weights` / `no_subjects_filled` / `ok`
  - แสดง chance pill + computed weighted ~XX + badge "ขาด N" ถ้า partial
  - Mobile: chance pill ใต้ "คะแนนต่ำสุด" ทางขวา
- Page `/analyze/major/[slug]` — compute relevant subjects union (union of all entries' weight codes) → pass ให้ MyScoreCard
- Page `/analyze/major` — เพิ่ม MyScoreCard summary mode บนสุด

### Files changed
```
src/lib/user-scores.ts                                                 (new — storage + hook)
src/features/compare/components/my-score-card.tsx                      (new — shared UI)
src/server/queries.ts                                                  (include requirement)
src/features/analyze/components/analyze-form.tsx                       (mirror write)
src/features/compare/components/major-comparison-table.tsx             (chance column)
src/app/analyze/major/[slug]/page.tsx                                  (MyScoreCard + relevantSubjects)
src/app/analyze/major/page.tsx                                         (MyScoreCard summary)
```

### Verification (live)
- ✅ `npx tsc --noEmit` ผ่านสะอาด
- ✅ `GET /analyze/major` → 200, แสดง "ยังไม่กรอกคะแนน" empty state ของ MyScoreCard
- ✅ `GET /analyze/major/[คณะแพทยศาสตร์]` → 200, มี column "โอกาส" ใน header (desktop + mobile)
- ✅ Flow ทดสอบ: ไปที่ /analyze กรอก TGAT1=75 → ดู localStorage มี `jknowledge:user-scores:v1` → ไปที่ /analyze/major/[slug] เห็นการคำนวณโอกาสแต่ละ uni

### Tradeoffs ที่ accept
- การคำนวณ chance ทำ client-side ทุกครั้งที่ user เปลี่ยน score → faster feedback แต่ component re-render
- เก็บคะแนนใน localStorage (ไม่ใช่ Dashboard ของ signed-in user) — privacy ดีกว่า แต่ cross-device ไม่ sync (defer ไว้ทีหลัง)
- Missing subjects แสดงเป็น "ขาด N" + ยังคำนวณ chance — ลด barrier ของ user แต่ค่าอาจไม่แม่นยำ

## — Reverted: /scores score-aware experiment
ผู้ใช้สั่ง revert การทดลอง score-aware บน /scores ทั้งหมด — ลบไฟล์ใหม่ทั้ง 3 (`src/lib/chance.ts`, `faculty-chance-chip.tsx`, `my-chance-card.tsx`), คืน `FacultyPreview` type / `getFacultiesByUniSlug` query / `FacultyList` / faculty detail page เป็นเวอร์ชันก่อน, restore inline `computeChance` ใน `major-comparison-table.tsx` (เคยถูก extract ไปยัง shared util)

State หลัง revert = เหมือนตอนหลัง Smart insights แต่ไม่มี /scores changes

---

## — Smart insights: rule-based commentary

### สิ่งที่ทำ
- เพิ่ม `src/lib/insights.ts` — pure rule-based helpers (no LLM):
  - `trendInsight(entries)` — เช็คค่าเฉลี่ย delta ของ minScore 2-3 ปีล่าสุดต่อ uni → "แข่งง่ายขึ้น / ยากขึ้น / เสถียร"
  - `scoreReachInsight(entries, userScores)` — นับ uni ที่ user มีโอกาส high/competitive ตามคะแนนใน localStorage
  - `rangeInsight(entries)` — บอกว่าช่วงคะแนนกว้าง (≥15) หรือใกล้กัน (≤5)
  - `computeMajorInsights(entries, userScores)` — public API คืน Insight[] ตามลำดับ priority
- เพิ่ม `InsightStrip` component — render insights แบบ grid 1-3 column ตาม count, 4 tone (good/celebrate/warn/info) ใช้ icon + bg ต่างกัน — ไม่มี red ตาม DESIGN.md
- เพิ่ม `MajorInsights` client wrapper — เรียก `useUserScores()` แล้ว recompute เมื่อ user กรอกคะแนน → insight "โอกาสสูง N" อัปเดต live
- เสียบที่ `/analyze/major/[slug]` ระหว่าง MyScoreCard กับ MajorComparisonChartLazy
- Bump cache key ของ `getMajorComparison` → "v2" (key derivation เปลี่ยน → cache "null" เก่าค้างทำให้ 404)

### Files changed
```
src/lib/insights.ts                                              (new)
src/features/compare/components/insight-strip.tsx                (new)
src/features/compare/components/major-insights.tsx               (new)
src/app/analyze/major/[slug]/page.tsx                            (insert MajorInsights)
src/server/queries.ts                                            (cache key bump)
```

### Verification (live)
- ✅ `npx tsc --noEmit` clean
- ✅ `GET /analyze/major/[วิศวกรรมโยธา]` → 200, render:
  - title: "วิศวกรรมโยธา · คณะวิศวกรรมศาสตร์ 16 มหาวิทยาลัย"
  - insight: "แนวโน้มคะแนนตัดเสถียร"
  - insight: "ช่วงคะแนนกว้าง 25.7 – ..."

### หมายเหตุ
- `scoreReachInsight` ต้อง user มีคะแนนใน localStorage ถึงจะ fire — ใน SSR initial render returns null, hydrate แล้วถ้ามีค่าค่อยขึ้น
- Insight ทุกตัวสามารถ skip ได้ (return null) — ถ้า data ไม่พอ ไม่ครบไม่ขึ้น noise

---

### Visual polish: full subject labels + color stripe
- ปัญหา: badge 2-3 อักษร (T1 / P3 / คณ1) ในแถวคะแนนซ้ำกับ label ที่ขึ้นต้นด้วย "TGAT1 ...", "A-Level ฟิสิกส์" อยู่แล้ว + cryptic + ขาด `gpax` ทำให้ฟอลล์แบ็คเป็น "GPA"
- เพิ่ม `gpax: "GPAX เกรดเฉลี่ยสะสม"` ใน `SUBJECT_LABELS` + เพิ่มเคส `gpax` → "GPAX" ใน `getSubjectShortCode` (chip mode)
- ลบ badge ออกจาก WeightedInputs (/analyze), CompareScoreInput (/analyze/compare), MyScoreCard (/analyze/major) — แทนด้วย **3px color stripe ทางซ้าย** ตาม group (TGAT 🔵 · TPAT 🟣 · A-Level 🟡)
- WeightedInputs: section header เดิมยังให้ context ของ group อยู่ → ลบ badge per-row, ไม่ต้องใส่ stripe
- CompareScoreInput + MyScoreCard input mode: flat list → ใส่ stripe per-row เพื่อยังคง color coding
- MyScoreCard summary chips ยังใช้ shortCode เพราะ chip เล็กเฉพาะตอน expanded
- Files: `src/lib/subjects.ts`, `weighted-inputs.tsx`, `compare-score-input.tsx`, `my-score-card.tsx`

### Bug fix: useSyncExternalStore "getSnapshot should be cached" infinite loop
- เวอร์ชันแรก `getSnapshot()` เรียก `readUserScores()` ที่ `JSON.parse` ใหม่ทุกครั้ง → reference เปลี่ยน → React re-render → ลูป
- แก้ด้วย module-level memoization: cache `parsed` ผูกกับ raw string จาก localStorage — เท่าเดิม return reference เดิม, ต่างเมื่อไรเพิ่ง re-parse
- `writeUserScores` invalidate cache + dispatch `CHANGE_EVENT` → subscriber รู้ → React เรียก getSnapshot อีกครั้ง → cache miss → re-parse → re-render หนึ่งครั้ง
- Legacy migration ย้ายเข้า `migrateLegacyIfNeeded()` ที่รันแค่ครั้งแรกและ persist ทันที (ของเก่า return state ที่ไม่ได้ persist — สับสน)

---

## — Major compare: canonical key fix + route move to /analyze/major

### Problem
User สังเกตว่าเมื่อคลิก "คณะวิศวกรรมศาสตร์" (34 มหาวิทยาลัย) ในหน้า popular → ตารางที่แสดงผลเป็น "วิศวะ" แต่ละมหาลัยเป็นคนละสาขา (โยธา/ไฟฟ้า/เครื่องกล) ไม่ใช่ apples-to-apples

Root cause: `getCanonicalMajorKey` ใช้แค่ `name + majorName` แต่ใน DB หลาย row มี `majorName=null` ทั้งที่ `program` ระบุสาขาไว้ ("วิศวกรรมศาสตรบัณฑิต (สาขาวิชาวิศวกรรมเครื่องกล)") → ถูกจัดอยู่ bucket เดียวกันทั้งที่จริงต่างสาขา + dedupe เลือกตาม scores.length → uni ละสาขาแบบสุ่ม

### Phase A: Canonical key includes program
- `getCanonicalMajorKey({name, program, majorName})` — specialty = majorName หรือ normalizedProgram (majorName ชนะถ้ามี)
- `CanonicalMajorParts.majorName` → rename เป็น `specialty` (clearer semantic)
- `getMajorComparison` — fetch by name ใน SQL, filter ด้วย canonical key ใน JS (เพราะ normalizeProgram เป็น regex op ไม่ SQL-friendly)
- `getPopularMajorSlugs` — เพิ่ม `program` ใน select + cache key bump `v2`
- เพิ่ม util `majorSlugFromFaculty(faculty)` — ครอบคลุม pattern ที่ใช้บ่อย

ผลลัพธ์: popular page เปลี่ยนจาก "คณะวิศวกรรมศาสตร์ 34 มหาวิทยาลัย" (mixed) → "วิศวกรรมโยธา · คณะวิศวกรรมศาสตร์ 28 มหาวิทยาลัย" + "วิศวกรรมไฟฟ้า · คณะวิศวกรรมศาสตร์ 22 มหาวิทยาลัย" (apples-to-apples)

### Phase B: Move route /analyze/compare/major/[slug] → /analyze/major[/slug]
- ลบทั้ง `src/app/analyze/compare/major/` directory
- สร้าง `src/app/analyze/major/page.tsx` — **index page ใหม่** แสดง popular majors (top 30) + help note อธิบายการจัดกลุ่ม
- สร้าง `src/app/analyze/major/[slug]/page.tsx` — เนื้อหาเดิม + breadcrumb กลับไป `/analyze/major` แทน `/analyze/compare`
- สร้าง `loading.tsx` ทั้ง 2 ระดับ
- อัปเดต:
  - `analyze-nav-items.ts`: 3rd item href → `/analyze/major` (มี URL ของตัวเองแล้ว)
  - `analyze-sub-nav.tsx`: `ANALYZE_SUB_TABS` = ทั้ง 3 items (เลิก slice เหลือ 2)
  - `result-card.tsx`: link → `/analyze/major/[slug]` + ใช้ `majorSlugFromFaculty()` ที่ส่ง program ด้วย
  - `popular-majors-section.tsx`: link → `/analyze/major/[slug]`
  - `sitemap.ts`: URL prefix + เพิ่ม `/analyze/major` ใน static routes

### Phase C: Cleanup /analyze/compare
- ลบ `PopularMajorsSection` ออกจาก `/analyze/compare` page (มี home ของตัวเองแล้วที่ `/analyze/major`)
- ลด query call จาก 3 → 2

### Files changed
```
src/lib/major-canonical.ts                                              (rewrite)
src/server/queries.ts                                                   (getMajorComparison + getPopularMajorSlugs)
src/app/analyze/major/page.tsx                                          (new — index)
src/app/analyze/major/loading.tsx                                       (new)
src/app/analyze/major/[slug]/page.tsx                                   (new)
src/app/analyze/major/[slug]/loading.tsx                                (new)
src/app/analyze/compare/major/                                          (deleted)
src/app/analyze/compare/page.tsx                                        (remove popular section)
src/components/layout/analyze-nav-items.ts                              (href + sub-tabs)
src/features/analyze/components/result-card.tsx                         (use majorSlugFromFaculty)
src/features/compare/components/popular-majors-section.tsx              (link prefix)
src/app/sitemap.ts                                                      (URL prefix)
```

### Verification (live)
- ✅ `GET /analyze/major` → 200 (index page)
- ✅ `GET /analyze/major/[slug]` → 200 (e.g. "คณะแพทยศาสตร์")
- ✅ `GET /analyze/compare/major/[slug]` → 404 (old route deleted)
- ✅ Popular chips show specialty: "วิศวกรรมโยธา · คณะวิศวกรรมศาสตร์", "เคมี · คณะวิทยาศาสตร์", etc.
- ✅ `npx tsc --noEmit` clean หลังลบ `.next/types` ที่ stale

### Breaking changes
- Old shared URL `/analyze/compare/major/[slug]` → 404 (slug format ก็เปลี่ยนเพราะ canonical key รวม program ด้วย)
- ผู้ใช้ที่ bookmark ไว้จะหา URL ใหม่ผ่าน nav แทน — Phase 2 เพิ่งปล่อย ไม่น่าจะมี bookmark จริง

---

## — Hybrid nav: dropdown + accordion + sub-tabs

### Problem
เพิ่ม "เปรียบเทียบ" เป็น top-nav ทำให้ปุ่มแน่นและ hierarchy ดูแบน — analyze + compare จริง ๆ เป็นพี่น้องของ family เดียวกัน

### สิ่งที่ทำ
- **Shared source** `src/components/layout/analyze-nav-items.ts` — list 3 sub-features (วิเคราะห์คะแนน / เปรียบเทียบหลายคณะ / คณะเดียวกันหลายมหาลัย) + helper `isAnalyzeItemActive`, `isAnalyzeGroupActive` — ใช้ร่วมกัน dropdown / accordion / sub-tabs
- **Desktop dropdown** (`nav-dropdown.tsx`) — ใช้ Base UI `Menu` component ได้ keyboard nav / ESC / focus management / click-outside ฟรี trigger เป็น "วิเคราะห์ ▾" rotate chevron เมื่อเปิด
- **Mobile drawer accordion** (`mobile-menu.tsx`) — เพิ่ม accordion "วิเคราะห์" ที่ขยายแสดง 3 sub-items default expanded ถ้า user อยู่ใน `/analyze*` แล้ว
- **AnalyzeSubNav** (`analyze-sub-nav.tsx`) — sticky pill tabs ใต้ header (top-16) แสดงในทุกหน้า `/analyze*` ใช้ `ANALYZE_SUB_TABS` (2 ตัวแรก — ไม่ใส่ "คณะเดียวกัน" เพราะ href ซ้ำกับ compare)
- **Popular majors section** (`popular-majors-section.tsx`) — server component แสดง 12 majors ยอดนิยมเป็น chip link ไปยัง `/analyze/compare/major/[slug]` — เป็น entry point ของ task ที่ 3 ใน dropdown
- Top nav header ลบ "เปรียบเทียบ" standalone, ใช้ `<NavDropdown label="วิเคราะห์" />` แทน

### Files changed
```
src/components/layout/analyze-nav-items.ts                  (new)
src/components/layout/nav-dropdown.tsx                      (new)
src/components/layout/analyze-sub-nav.tsx                   (new)
src/components/layout/header.tsx                            (use NavDropdown)
src/components/layout/mobile-menu.tsx                       (accordion)
src/features/compare/components/popular-majors-section.tsx  (new)
src/app/analyze/page.tsx                                    (+ sub-nav)
src/app/analyze/compare/page.tsx                            (+ sub-nav, + popular majors, fetch slugs)
src/app/analyze/compare/major/[slug]/page.tsx               (+ sub-nav)
```

### หมายเหตุ
- ตรวจ `npx tsc --noEmit` ผ่านสะอาด
- ทดสอบ live ที่ http://localhost:3000:
  - `/analyze` → 200, dropdown trigger "วิเคราะห์" ปรากฏ 2 ครั้ง (header + sub-nav heading area), sub-tab "เปรียบเทียบหลายคณะ" present
  - `/analyze/compare` → 200, "คณะเดียวกัน หลายมหาวิทยาลัย" section + chip "มหาวิทยาลัย" หลายตัว present


---

## — Nav menu: expose compare features

### สิ่งที่ทำ
- เพิ่ม "เปรียบเทียบ" ใน `NAV_LINKS` ของ header → ทั้ง desktop nav และ mobile drawer pick ขึ้นพร้อมกัน (ใช้ source เดียว)
- เพิ่ม flag `exact?: boolean` ใน NavLink type + helper `isLinkActive(pathname, href, exact)`:
  - `/analyze` ใช้ `exact: true` → highlight เฉพาะ exact match ไม่ติดเมื่ออยู่ `/analyze/compare`
  - `/analyze/compare` ใช้ startsWith → highlight ทั้ง `/analyze/compare` และ `/analyze/compare/major/[slug]`
- เพิ่ม link "เปรียบเทียบคณะ" ใน footer ของหน้าแรก
- ไม่แตะ BottomNav (มี 5 ไอเทมเต็มแล้ว — Home/วิเคราะห์/คะแนน/MBTI/Folio) — ผู้ใช้บนมือถือเข้าผ่าน hamburger drawer หรือ in-page CTA จาก ResultCard ได้

### Files changed
```
src/components/layout/header.tsx
src/components/layout/mobile-menu.tsx
src/app/page.tsx                     (footer link)
```

### หมายเหตุ
- ตรวจ `npx tsc --noEmit` ผ่านสะอาด

---

## — Phase 2: Same Major Across Universities

### Goal
หน้าใหม่ `/analyze/compare/major/[slug]` — เลือกคณะ X แล้วเห็นทุกมหาลัยที่เปิดสอน คณะนี้ พร้อมคะแนน เรียงจากเข้าง่ายสุด เป็น SEO play เพราะตอบคำถาม "เรียนคณะ X ที่ไหนคะแนนต่ำสุด"

### สิ่งที่ทำ
- **Canonical major key util** (`src/lib/major-canonical.ts`)
  - `getCanonicalMajorKey({name, majorName})` — exact name + majorName หลัง trim whitespace
  - `majorSlugFromKey(key)` / `parseMajorSlug(slug)` — URL round-trip ด้วย encodeURIComponent (รองรับ Thai)
  - `formatMajorLabel(parts)` — display
  - MVP: exact match — รองรับเคสหลักทั้ง "คณะแพทยศาสตร์" (no major) และ "วิศวกรรมคอมพิวเตอร์" (with major)
  - Known limit: ชื่อคณะ variant ("คณะแพทยศาสตร์ ศิริราช" vs "คณะแพทยศาสตร์") จะแยกกัน — defer ไปยัง synonym map ทีหลัง
- **Server queries** (`src/server/queries.ts`)
  - `getMajorComparison(slug)` — รวมทุก Faculty ที่ name+majorName ตรง dedupe ตาม uni (เลือก row ที่มีคะแนนเยอะสุด) เรียง min asc มี `unstable_cache` revalidate 3600
  - `getPopularMajorSlugs(min=3, limit=60)` — major ที่มี ≥ 3 มหาลัยเปิด สำหรับ sitemap
- **Components**
  - `MajorComparisonChart` + lazy wrapper — Recharts horizontal bar, สีตาม curated palette แต่ละแถบ มี tooltip ระบุ ที่นั่ง+ตำแหน่ง dynamic height ตามจำนวน entries
  - `MajorComparisonTable` — sortable header (min/seats/shortName), mini-bar inline ใน column ต่ำสุด, mobile = compact card stack, แต่ละ row link ไป `/scores/[uniSlug]/[facultyId]`
- **Route** `/analyze/compare/major/[slug]/page.tsx` + `loading.tsx`
  - `generateMetadata` — title/description/OG/canonical ตาม slug
  - Hero มี breadcrumb, label, summary chips (uniCount, total seats, easiest, hardest)
  - JSON-LD `ItemList` ส่งให้ Google rich result
  - notFound() ถ้าไม่พบหรือไม่มี entries
- **Sitemap** — เพิ่ม `/analyze/compare/major/<slug>` ตาม popular majors, priority scale ตาม uniCount (max 0.8), + `/analyze/compare` ใน static routes
- **ResultCard CTA** (`result-card.tsx`) — ปุ่ม "ดูคณะนี้ที่มหาลัยอื่น" link จาก analyze result โดยคำนวณ slug จาก faculty.name + majorName

### Files changed
```
src/lib/major-canonical.ts                                              (new)
src/server/queries.ts                                                   (+ 2 queries)
src/app/analyze/compare/major/[slug]/page.tsx                           (new)
src/app/analyze/compare/major/[slug]/loading.tsx                        (new)
src/features/compare/components/major-comparison-chart.tsx              (new)
src/features/compare/components/major-comparison-chart-lazy.tsx         (new)
src/features/compare/components/major-comparison-table.tsx              (new)
src/app/sitemap.ts                                                      (+ popular majors)
src/features/analyze/components/result-card.tsx                         (+ major-compare CTA)
```

### Tasks deferred to phase 2.x
- Synonym/normalization map (variant names) — MVP exact match ใช้ได้กับเคสหลัก
- Cross-link จาก /scores/[uniSlug] หรือ /scores/[uniSlug]/[facultyId] — ผู้ใช้เข้าผ่าน /analyze ได้แล้ว
- /tcas/min-scores landing page ที่ list popular majors เป็น static SEO doorway

### หมายเหตุ
- ตรวจ `npx tsc --noEmit` ผ่านสะอาด
- Port 3000 ถูก dev server ผู้ใช้ใช้อยู่ — Next.js HMR pick ไฟล์ใหม่ ทดสอบโดย: (1) ไปที่ `/analyze` วิเคราะห์คณะใดๆ → คลิก "ดูคณะนี้ที่มหาลัยอื่น" (2) sitemap: `curl http://localhost:3000/sitemap.xml` แล้วเช็คว่ามี `/analyze/compare/major/...` (3) ลองเปิดตรง: `/analyze/compare/major/%E0%B8%84%E0%B8%93%E0%B8%B0%E0%B9%81%E0%B8%9E%E0%B8%97%E0%B8%A2%E0%B8%A8%E0%B8%B2%E0%B8%AA%E0%B8%95%E0%B8%A3%E0%B9%8C` (= "คณะแพทยศาสตร์")


---

## 15:22

### Files changed
```
src/app/page.tsx
src/app/sign-in/[[...sign-in]]/page.tsx
src/app/sign-in/sso-callback/page.tsx
src/app/sign-up/[[...sign-up]]/page.tsx
src/app/sign-up/sso-callback/page.tsx
src/app/sitemap.ts
src/components/layout/header.tsx
src/components/layout/mobile-menu.tsx
src/features/analyze/components/analyze-form.tsx
src/features/analyze/components/result-card.tsx
src/features/auth/lib/sso-finalize.ts
src/features/auth/lib/validation.ts
src/features/tcas-folio/components/pdf-preview-card.tsx
src/lib/analytics.ts
src/server/actions.ts
src/server/queries.ts
src/types/tcas.ts
```

